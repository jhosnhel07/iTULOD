.env
.env.*
secrets.env
**/secrets.env
config.secrets.js
**/config.secrets.js

# Supabase CLI local state (created by `supabase init` / `supabase link`)
**/supabase/.temp/
**/supabase/.branches/

# Node dependencies (tests only — the app ships no build step)
**/node_modules/
npm-debug.log*

# Claude Code local machine settings (may contain secrets in allow-list entries)
.claude/settings.local.json
